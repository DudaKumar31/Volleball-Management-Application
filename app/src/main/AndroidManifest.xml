<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.volleyball">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.GetAllSeasonsActivity"/>
        <activity android:name=".activities.EditTeamsActivity" />
        <activity android:name=".activities.CreateTeamActivity" />
        <activity android:name=".activities.AllTeamsActivity" />
        <activity android:name=".activities.AllSeasonsActivity" />
        <activity android:name=".activities.EditSeasonsActivity" />
        <activity android:name=".activities.CreateManagersActivity" />
        <activity android:name=".activities.CreateSeasonsActivity" />
        <activity android:name=".activities.UserDashBoardActivity" />
        <activity android:name=".activities.LeagueManagerDashBoardActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>